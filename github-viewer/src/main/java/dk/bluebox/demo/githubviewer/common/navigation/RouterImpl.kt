package dk.bluebox.demo.githubviewer.common.navigation

import androidx.appcompat.app.AppCompatActivity

internal class RouterImpl(private val activity: AppCompatActivity) : Router {
    override fun goToDetails(id: Long) { /* doe not do anything */ }
}
